<nav class="navbar navbar-expand-lg navbar-dark sticky-top" style="background: linear-gradient(135deg, #1a3a5c 0%, #2563eb 100%); box-shadow: 0 2px 20px rgba(37,99,235,0.3);">
  <div class="container-fluid px-4">
    <a class="navbar-brand d-flex align-items-center gap-2 fw-bold fs-4" href="/dashboard">
      <div class="brand-icon d-flex align-items-center justify-content-center rounded-2" style="width:36px;height:36px;background:rgba(255,255,255,0.2);">
        <i class="bi bi-calendar2-check text-white fs-5"></i>
      </div>
      <span class="text-white">Attent<span style="color:#93c5fd;">75</span></span>
    </a>
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav mx-auto gap-1">
        <li class="nav-item">
          <a class="nav-link px-3 py-2 rounded-2 d-flex align-items-center gap-2 <%= typeof page !== 'undefined' && page === 'dashboard' ? 'active bg-white bg-opacity-10' : '' %>" href="/dashboard">
            <i class="bi bi-speedometer2"></i> Dashboard
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3 py-2 rounded-2 d-flex align-items-center gap-2 <%= typeof page !== 'undefined' && page === 'subjects' ? 'active bg-white bg-opacity-10' : '' %>" href="/subjects">
            <i class="bi bi-book"></i> Subjects
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3 py-2 rounded-2 d-flex align-items-center gap-2 <%= typeof page !== 'undefined' && page === 'attendance' ? 'active bg-white bg-opacity-10' : '' %>" href="/attendance/mark">
            <i class="bi bi-clipboard2-check"></i> Attendance
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-3 py-2 rounded-2 d-flex align-items-center gap-2 <%= typeof page !== 'undefined' && page === 'pyq' ? 'active bg-white bg-opacity-10' : '' %>" href="/pyq">
            <i class="bi bi-file-earmark-text"></i> PYQ
          </a>
        </li>
      </ul>
      <div class="d-flex align-items-center gap-3">
        <div class="dropdown">
          <button class="btn btn-link p-0 d-flex align-items-center gap-2 text-decoration-none" data-bs-toggle="dropdown">
            <div class="avatar rounded-circle d-flex align-items-center justify-content-center text-white fw-bold" style="width:38px;height:38px;background:rgba(255,255,255,0.25);font-size:15px;">
              <%= typeof user !== 'undefined' ? user.username.charAt(0).toUpperCase() : 'R' %>
            </div>
            <span class="text-white d-none d-lg-inline fw-medium"><%= typeof user !== 'undefined' ? user.username : 'Rahul' %></span>
            <i class="bi bi-chevron-down text-white-50 small"></i>
          </button>
          <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-3 mt-2">
            <li><a class="dropdown-item py-2" href="/semester/setup"><i class="bi bi-gear me-2 text-primary"></i>Semester Setup</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item py-2 text-danger" href="/logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>
